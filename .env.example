# CodeAudit Agent 环境变量示例：复制为 .env（已 gitignore）或直接 export 到 shell。
# 三个变量均对应 audit/config.py 的 AuditConfig.from_env；不设置 GLM_API_KEY 时自动进入纯规则离线模式。

# 智谱开放平台 API Key（必填才能启用 LLM 审查/复核/修复/单测生成；留空 = 离线纯规则模式，不发起任何网络请求）
GLM_API_KEY=

# OpenAI 兼容接口地址（可选；默认智谱开放平台 v4 端点）
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# 模型名（可选；默认 glm-5.3-flash，需支持 Function Calling 与 JSON 输出）
GLM_MODEL=glm-5.3-flash
