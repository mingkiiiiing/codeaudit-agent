# 第十轮全维系统性审计报告（2026-09-21）

> 审计对象：W29 收口后的工作区实态——**实际发现工作区已含 W30 增量**（docs/29 四卡：污点传播 MVP / rename 类型推断 / SECRET 注释掩码 / 前端 rename 工具），且 **W30 处于半收口状态**：四卡代码与测试已落库，但集成收口件（registry 接线 / rules.md / CHANGELOG / docs §4 / 记忆同步）未完成。本轮为该状态下的首次全维审计。
> 方法：全套指标现场复跑（不引历史记录）+ W30 四卡逐卡走读与行为实证 + 上轮 P0 复核。每个判断附 文件:行号 证据。

## 1. 评分

**综合 74/100（B）（加权 73.8）**——功能 74（35%）/ UX 69（20%）/ 流程 68（20%）/ 架构 82（25%）。

较第九轮（74.00，功能 74/UX 68/流程 70/架构 82）实质 **-0.2**：W30 四卡**实现质量全部站住**（连续三轮审计零新实现级缺陷），rename 三放宽/掩码/前端三项能力真实可达（UX +1）；但 W30 会话中断留下**首例半收口状态**——污点规则未接线主链路（P1）+ 收口件三不同步 + 全量 pytest 首次带红（流程 -2）。若 F10-R1 半小时级清偿并收口，预计回到 ~74.8。

## 2. 实测记录（全部现场复跑）

| 指标 | 结果 | 判定 |
|---|---|---|
| 全量 pytest | **1 failed, 2515 passed, 2 skipped（600.50s）** | ⚠ 首次带红（见 F10-R1） |
| 测试对账 | 2382（W29 基线）+ 134（W30 四卡）= 2516 collected，逐位成立 | ✅ |
| ruff 全域（audit/cli/server/bench/tests） | **1 错**：`registry.py:15 F401 build_py_taint_rules imported but unused` | ⚠（F10-R1 静态证据） |
| demo 闭环 | verified，10.1s，生成单测通过 | ✅ |
| 金标（--offline 一条命令，10 项目 240 标签） | **P=1.000 / R=0.844 / F1=0.916**，报告 333 条，逐位一致（audit_round10_goldset_20260921.md） | ✅ **零回退第十连** |
| GLM 探针（第十一次） | **ConnectError：open.bigmodel.cn TLS UNEXPECTED_EOF**（2 次尝试 19.3s；非 429） | ⚠ 定性变化（F10-R3） |
| soak 36/36 收口 | **29 PASS / 7 FAIL / 0 崩溃**，1332 任务零 5xx 零丢失；7 FAIL 归因闭环=本机 fuwai 负载×固定 300s 窗口，负载消退末轮即恢复 PASS；RSS 跨轮平台 154.5~164.9MB 十余小时零爬升=无泄漏 | ✅（soak_long_w21_巡检.md 总汇） |
| rename 三放宽 E2E | (a) `obj=ClassName()` 构造赋值放行 / (b) `super().X` 基类链放行 / (c) 两层基类链传递闭包放行；**(d) 参数传入歧义仍拒含位置明细 / (e) 继承环 A→B→A 不挂死按不可解析拒绝** | ✅ 放宽生效且安全边界保持 |
| taint 主链路 | `rules_for("python")` 52 条**无** PY-TAINT-UNSAFE-SINK；规则直跑同语料命中 1（sink 行） | ❌ F10-R1 |
| SECRET 掩码行为 | `# password = "..."` 注释假声明不报；`API_KEY = "sk-..."` 真实赋值仍报（masked 行为实证：注释行全空白、赋值行保留 `NAME = "` 结构） | ✅ |
| doctor | 10 OK / 1 警告（docker 未装，opt-in 合理）/ 0 失败；六语言包全探测 | ✅ |
| 前端三件套 | tsc 0 错；vitest **14 文件 83 passed**（W29 76 + W30 RenameTool 7）；build 24.84s（chunk 警告为既有非阻断） | ✅ |

## 3. W30 四卡复审详情

### 卡 A 污点传播 PY-TAINT-UNSAFE-SINK —— 规则本体通过，接线断（F10-R1）

- **本体质量高**：py_taint.py docstring 边界如实申报（跨函数/别名导入/属性链中间污染/for-with-except-walrus 绑定不做、execute 只查第一实参防参数化误报）；AST-only 契约沿 PY-NONE-DEREF 先例（tree=None 不产命中）；测试 24 正例+12 对照内联语料以 `# SINK` 标记精确断言行号（W18 教训内化）。
- **接线断**：`audit/detect/registry.py:15` import 了 `build_py_taint_rules` 但全文件无 `register_all` 调用（对比 W23-B none_deref `registry.py:50`、W24 java `:52`、W26 go `:54`、W28 cpp `:56` 均有）。registry 实测 92 条（方案声称 93）。engine 全链路经 `registry.rules_for(rc.language)` 消费（engine.py:313、engine.py:401）——CLI/server/CI/前端四入口全部不可达。test_py_taint.py 直接 import 规则类自测不经 registry，故测试绿而主链路缺。
- **三重证据一致**：动态（92 条/无 TAINT in rules_for）+ 静态（ruff F401）+ 测试体系（test_ci_config.py::test_ruff_check_zero_errors 如实红）。CI 看门有效但未拦截入树。

### 卡 B rename 类型推断三放宽 —— 通过（本轮质量亮点）

- 实现严谨（rename.py）：放宽① `_nearest_direct_construction_class`（:375）仅认 Name 左值+裸类名调用、赋值与引用同函数体（嵌套函数体内赋值排除 :394）、引用点之前（:391）；放宽② `_is_zero_arg_super_call`（:361）带参 `super(C, self)` 不认；放宽③ `_chain_hits_definition`（:334）传递闭包 + visited 环防护（:347-348 重复访问即 False 不挂死）+ 同名类多定义剔除（_collect_class_definitions :309 duplicated 集）+ 跨文件基类不展开按不可解析。
- E2E 五场景全过（§2）：三放宽生效、歧义仍拒含 file:line 明细、继承环不挂死。W29 17 用例零改动（test_rename_scope.py 计数 17 未变，硬约束达成）。
- 安全论证成立：所有放宽都把"可静态判定"的引用点从歧义转为归属，判定语义是"确认全部引用都确定指向定义点集合之一才放行全量替换"（rename.py:72-75）——未削弱「宁拒不改」，只缩小误拒面。

### 卡 C SECRET 注释掩码 —— 通过

四语言（python.py:1395 区/1461、java.py:353/385、go.py:348/381、cpp.py:359/391）同构落地：raw 行命中 `_ASSIGN_RE` 后同正则在 masked 行复验，masked 不再匹配 ⇒ 注释内假声明 ⇒ 跳过。判定闸门（R1-9/R4-1/R4-7）零变化。行为实证：注释假声明不报、真实赋值仍报。**假阴性风险论证**：真实赋值行的 `NAME = "` 在 masked 行结构保留（引号定界符不 mask），唯一同时被 mask 掉赋值形态又含敏感值的场景是三引号字符串中间行的假声明——本就不该报。假阴性面可忽略。

### 卡 D 前端 rename 工具 —— 通过

RenameTool.tsx 两段式强制（preview 为 null 时应用按钮 disabled :147）、表单变更即 resetResults 失效旧预览（:39-43，防 stale preview 后应用的竞态）、409 附 all-or-nothing 说明（:163-167）。挂载完整（App.tsx:17 /rename 路由、BasicLayout.tsx:9/21-22/55 导航、client.ts:192 renameSymbol）。language 透传缺省与 W29 server 缺省 python 对齐。

## 4. 缺陷清单

### F10-R1（P1，本轮最重要产出）：W30 污点传播规则未接线主链路

- **触发条件**：任何用户入口（`codeaudit run` / server / CI / 前端）对含污点传播漏洞的代码审计。
- **预期行为**：PY-TAINT-UNSAFE-SINK 命中（方案 docs/29 卡 A 验收口径：正例 ≥20 检出 ≥15）。
- **实际行为**：零命中（规则不在 registry，engine 不执行）。规则库对外声称 93 条实际 92 条；docs-site/rules.md 无 TAINT 条目（与"已实现"的代码状态不一致——幸而文档也未同步，未形成"文档声称但代码没有"的反向失真）。
- **证据**：registry.py:15（import 无调用）vs registry.py:50-56（五先例均有 register_all）；`len(reg.all_rules)==92`；`"PY-TAINT-UNSAFE-SINK" not in rules_for("python")`；ruff F401；pytest 1 failed。
- **同型先例**：W27-C safe-rename MVP 落地未接线（第八轮审计 P0 存量首位"MVP 能力用户不可达"）——本型缺陷第二次出现，且均发生在"纯新模块+自测不经主链路"的卡片形态。**改进建议**：新规则卡模板把"registry register_all + test_rules_ext 计数断言 + rules.md 再生"固化为卡内 checklist 而非集成人收口件（集成人收口是单点，会话中断即断链）。
- **修复**（半小时~1 小时级）：registry.py 补一行 register_all → test_rules_ext 断言 92→93 → rules.md 再生 → **金标零回退复验**（taint 上线可能引入新命中，需验证 240 金标无污染；语料含 request/execute 形态的项目理论上有新增命中面，预期 P 不降）→ demo 回归。

### F10-R2（P2）：W30 集成收口件整体缺失（半收口状态）

CHANGELOG 无 Wave 30 段（最新 Wave 29，CHANGELOG.md:49）；docs/29 仅方案无 §4 联调收口章节；记忆锚点停在 W29（"W24-E~W29 待提交"），与工作区实态（含 W30）三不同步。后果已实证：本轮审计首访即以记忆锚点误判对象范围，需现场重校。**修复**：随 F10-R1 一并做收口（CHANGELOG 段 + docs/29 §4/§5 + 记忆同步），下轮工作轮第一张卡。

### F10-R3（P3，定性更新）：GLM 端点不可达性变化

第十一次探针**非 429 而是 ConnectError**（`https://open.bigmodel.cn/api/paas/v4` TLS UNEXPECTED_EOF_WHILE_READING，httpx 直连复现）。前十次探针均为 429 限流，本次为网络层 TLS 中断——P0-1 对数依旧阻塞，但定性从"等配额窗口"更新为"**本机网络到端点不可达**"（代理/防火墙/服务端任一可能），建议用户侧核实网络环境后再探。若端点恢复且 429 解除，直接重跑 `python -m bench.w22_focus_compare`（docs/21 §5 判据）。

## 5. P0 现役清单与下轮路由

| 项 | 状态 | 说明 |
|---|---|---|
| **P0-13（新）** | 待做（半小时~1h） | F10-R1 taint 接线 + F10-R2 W30 收口件补全 + 金标零回退复验——**下轮工作轮首选** |
| P0-1 | 阻塞（定性更新） | F10-R3 网络层不可达；端点恢复后按 docs/21 §5 对数 |
| ~~rename 类型推断~~ | ✅ 已清偿 | W30 卡 B 落地且实测通过（本轮 E2E 五场景） |
| ~~SECRET 注释掩码~~ | ✅ 已清偿 | W30 卡 C 四语言落地且行为正确 |
| ~~前端 rename 入口~~ | ✅ 已清偿 | W30 卡 D 上线（三件套绿） |
| 催办 | 待用户 | **W24-E~W30 积压提交 + push + 打版 0.7.0**（连续多轮；提交前建议先清 P0-13，避免 92≠93 入库） |

**下轮路由 = 本对话或新开对话贴提示词二（工作轮），首选 P0-13**（唯一 P1 且半小时级，收口后全量应回全绿 2517+ 量级）。

## 6. 对标参照（业界 ≥2 系统）

- **规则接线完整性**：Semgrep / Grep 的 CI 集成在规则加载为 0 或少于配置预期时会显式告警（fail-fast 哲学）；CodeAudit 的 registry 静默接受少注册（本次靠 ruff F401 间接暴露 + CI 看门测试红兜底——两道防线都工作了，但都是"事后红"而非"接入即拦"）。改进候选：register_all 后的计数断言前移到 registry.py 模块级（import 时自检），把"断链"从测试期提前到导入期。
- **半收口状态治理**：GitHub draft PR / GitLab WIP MR 以显式标记阻止未完成工作合入主干；本项目工作区长期承载多波次未提交改动（当前 ~110 改动 + 40 新文件），无"半收口"显式标记机制，仅靠 pytest/ruff 红绿事实门禁——门禁有效（本次证明）但无预警。改进候选：W 波次收口 checklist 中的"CHANGELOG 段+docs §4+记忆"三件未齐时，在 docs 标题加"（未收口）"后缀。

## 7. 收尾声明

- 本轮**通过**：金标十连零回退、demo verified、rename 三放宽 E2E、SECRET 掩码行为、doctor、前端三件套、taint 规则本体（直跑）。
- 本轮**失败（如实）**：全量 pytest 1 failed（test_ruff_check_zero_errors，根因 F10-R1）；ruff 全域 1 错（F401，同根因）；GLM 探针 ConnectError。
- **未执行**：soak 新轮（drain 上线后 FAIL 消失对照——36 轮已收官且 drain 语义已由 W29 首验覆盖，新 soak 待下轮工作轮顺带）；taint 上线后的金标复验（依赖 F10-R1 修复）；在线对数（端点不可达）。
- 不 commit、不 push；契约文件零改动；全程未并行写 .codeaudit 的手动验证（pytest 期间仅只读走读+探针）。
