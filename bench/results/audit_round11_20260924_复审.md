# 第十一轮全维审计报告（2026-09-24）

> 审计对象：工作区（bb471c0 之后 W24-E~W30 + P0-13 收口态，105 个未提交改动）
> 触发：用户指令「全方位审查+测试」。上轮（第十轮，09-21）74/100 B（加权 73.8），F10-R1（P1）taint 未接线 → P0-13 工作轮同日收口。本轮复审重点：①P0-13 实测抽核与守卫有效性；②全量指标复跑；③GLM 网络定性核实；④已申报遗留复核。
> 方法：现场重校（git/registry 动态计数）+ 全链实测复跑 + P0-13 核心落点走读 + W30 面 E2E 抽测。每个判断附 文件:行号 证据。

## 1. 现场校验

- `git status -sb`：`main...origin/main [ahead 1]`（bb471c0 已提交未 push），工作区 105 个改动——与记忆锚点（W24-E~W30 + P0-13 收口态）一致，无会话外新改动。
- 自动化环境：仅剩 soak 巡检（automation-e1bd3b2d，36/36 完成停用 enabled=false）；每小时空转自动化此前已停用。本轮全部实测无并发干扰（全量 pytest 期间零并行写 `.codeaudit` 的手动命令，W26 教训执行到位）。

## 2. P0-13 抽核（上轮 F10-R1/F10-R2 清偿验证）——全部通过

| 抽核项 | 证据 | 结果 |
| --- | --- | --- |
| registry 接线 | `audit/detect/registry.py:58` `register_all(build_py_taint_rules())`，import 与注册成对 | ✅ |
| 动态计数 | 实测 `DEFAULT_REGISTRY.all_rules` = **93 条（唯一 93）**；`rules_for("python")` = 53 条含 `PY-TAINT-UNSAFE-SINK` | ✅ 与方案/文档逐位一致 |
| 接线守卫 | `tests/unit/detect/test_registry_wiring.py`：pkgutil 遍历 rules 包树全部 `build_*_rules` 工厂（覆盖下限 ≥15，当前 20），逐工厂隔离注册后对照 DEFAULT_REGISTRY——「import 了但没注册」的 F10-R1 形态接入期即红 | ✅ 走读通过（变异验证系 P0-13 收口期实测：屏蔽 taint 注册行→红，恢复→绿，本轮引用该记录） |
| 计数断言同步 | test_rules_ext W30 段+总数 ==93、cpp:248 / java:216 / go:272 三处 ==93（全量 pytest 内覆盖） | ✅ |
| rules.md 再生 | 93 条；总表 Python 53 条；`PY-TAINT-UNSAFE-SINK` 在总表（L79）与明细（L1298） | ✅ |
| 收口三件套 | CHANGELOG Wave 30 段（L11-18 含 P0-13 集成收口记录）/ README L25 93 条六语言口径 / docs/29 §4 实测记录完整 | ✅ |

**结论：F10-R1/F10-R2 清偿经独立抽核成立，守卫资产真实有效。**

## 3. 全链实测复跑（全部现场执行）

| 指标 | 实测 | 对比基线（P0-13 收口 09-21） | 判定 |
| --- | --- | --- | --- |
| 全量 pytest | **2518 passed + 2 skipped（370.18s，exit=0）** | 2518+2（429.88s） | ✅ 逐位一致，全绿保持 |
| ruff 全域（audit/cli.py/server/bench/tests） | **0 错** | 0 错 | ✅ |
| demo 闭环 | **verified，12.7s，exit=0** | verified 21.5s | ✅ |
| 金标 240 条（--offline 一条命令） | **P=1.000 / R=0.844 / F1=0.916，报告 333 条** | 同口径逐位一致（w30_goldset_20260921.md） | ✅ **零回退连续十二轮**（taint 上线金标零污染持续成立） |
| doctor | **10 OK / 1 警告（Docker 未装）/ 0 失败**，六语言包（py/js/ts/java/go/cpp）全探测 | 10OK | ✅ |
| 前端三件套 | tsc **0 错** / vitest **83 passed**（44.6s）/ build **绿（20.8s）** | 同 | ✅ |
| registry 动态 | 93 条唯一 / python 53 / TAINT 可达 | 同 | ✅ |

金标准确命令（README「指标复现」节，`--offline` 透传 `api_key=''` 防 .env 混入）：结果文件 `bench/results/audit_round11_goldset_20260924.md`。

## 4. W30 面轻量复访（走读 + E2E 抽测）

- **py_taint.py 走读**：AST-only 兜底（`py_taint.py:256-257` tree=None 返回空）；值位置口径防假阳（`:158-192` 属性链只查 object、调用只查派生对象与实参、关键字实参只查 value）；SQL 只查首实参——参数化查询不误报（`:413`）；洗白语义（再赋非污点值即出污染集，`:341-342`）；同行 sink 去重（`:415`）；边界 10 条 docstring 如实申报。与第十轮实测（金标零污染）互证，无新缺陷。
- **rename 类型推断 E2E 抽测（本轮现场构造语料）**：①`reg = Registry()` 构造赋值回溯 → `reg.lookup` 放行，计划 OK（定义点 1、替换点 2）——W30-B 放宽①生效；②同名方法跨类 + 参数传入 → **整体拒绝**，错误含位置明细（`amb.py:9 x.get 形态（接收者类型不可静态判定）`）——「宁拒不改」安全边界保持。
- **SECRET 注释掩码**：本轮未单独重跑行为脚本，由全量套件内 `test_secret_comment_mask.py`（四语言 `_is_comment_only` 用例）覆盖——如实标注。

## 5. 缺陷与定性更新

**本轮走读零新实质缺陷**（第十轮 F10-R1 之后，工作区无新代码面；唯一观察项为已申报遗留）。

- **F11-R1（P2，已申报遗留，非新发现）**：`scripts/gen_rule_docs.py:28` `LANG_ORDER = ("python", "javascript", "typescript", "java")` 不含 go/cpp——rules.md 规则总表仅四语言节（Python 53/JS 24/TS 16/Java 3），**Go/C++ 6 条仅进「规则明细」（字典序 L180-227）而无总表小节**，统计节（L1542）同不含 go/cpp；与 README「六语言口径」呈现不一致。触发条件：读者在规则手册总表按语言找 Go/C++ 规则；预期：六语言各一节；实际：缺两节。docs/29 §4 已如实申报（「W28 遗留失真，留待文档轮顺带」「doctor 探测/rules.md 总表/gen_rule_docs 三者口径不一，随打版前文档统一收口」）。修复量级：半小时（LANG_ORDER 扩两语言 + 再生 + doctor 口径核对）。
- **F11-R2（P3，定性更新）**：GLM 单点探针**第十二次**——上一轮 ConnectError（网络层不可达）**已恢复**：HTTP 2.1s 应答，但回 **429 `余额不足或无可用资源包`**（open.bigmodel.cn 错误码 1113）。P0-1（w22_focus_compare 对数）阻塞定性从「网络不可达」修正回「余额不足」；网络层无需再核实。

## 6. 四维评分

| 维度 | 权重 | 上轮 | 本轮 | 变化依据 |
| --- | --- | --- | --- | --- |
| 功能实现 | 35% | 74 | **75** | taint 经 P0-13 真正可达（93 条），「数据流级安全」从方案变可用能力；守卫防再犯。扣分维持：跨函数/跨文件污点、多语言语义索引等代际缺口未动，LLM 在线通道阻塞 |
| 用户体验 | 20% | 69 | **69** | 无新 UX 面（W30 前端 rename 已计入上轮）；rules.md Go/C++ 总表缺失为文档呈现项（计入流程/已知项） |
| 流程优化 | 20% | 68 | **69** | 接线口径从「集成人单点收口」前移为「守卫测试 + 卡内 checklist」（F10-R1 教训固化）；docs/29 收口记档完整、遗留如实申报——工程流程质量实质提升 |
| 系统架构 | 25% | 82 | **82** | 无架构面变化；分层/契约/测试资产维持 |

**综合：74/100（B，加权 74.35，实质 +0.55）。** 十一轮轨迹：66 → 70 → 71 → 72 → 73 → 73.45 → 74 → 74.00 → 73.8 → 73.8 → **74.35**。

## 7. 业界对标（沿袭前十轮框架）

- **Semgrep（污点分析标杆）**：其 taint mode 支持跨函数/跨文件/过程间摘要与 sanitizer；本项目 PY-TAINT-UNSAFE-SINK 为「函数内保守子集」（source/sink 固定形态、线性源码序），定位是规则通道内零成本的第一步，边界 docstring 全申报——方向正确、代际差距明确。
- **JetBrains IDE Rename**：跨项目全域解析、类型推断完整；本项目 safe-rename 经 W30 三放宽（构造赋值回溯/super 链/传递闭包）误拒面收窄且歧义仍拒——确定性原语的安全优先口径持续站住。
- **CI 质量门禁惯例（如 Google Tricursor 的「提交前可验证」理念）**：`test_registry_wiring.py` 把「接线完整性」从人工 checklist 变测试资产（pkgutil 动态覆盖新工厂），符合「让错误无法入库」的门禁设计而非事后发现。

## 8. P0 现役清单与下轮路由

1. **P0-1**（阻塞，第十二次探针 429 余额）：`python -m bench.w22_focus_compare` 对数（W22-A/B/F 翻默认判据 P50≤300s/KLOC 且 P 降幅≤3pp）——待余额窗口。
2. **P0-14（新立，P2）**：文档统一收口卡——`gen_rule_docs.py` LANG_ORDER 扩 go/cpp + rules.md 再生 + doctor/_DOCTOR_LANGS 口径核对（F11-R1；docs/29 已预告「随打版前文档统一收口」）。半小时级。
3. 存量代际：跨函数污点（过程间摘要）、rename 跨文件构造赋值回溯、多语言语义索引。
4. **催办（连续多轮）**：**W24-E~W30 提交 + push + 打版 0.7.0**——工作区已全绿（本轮全指标复核通过：2518+2 / ruff 0 / 金标十二连 / demo verified / 前端三件套绿），93 条口径全链一致，**具备提交条件**；建议打版前顺手清 P0-14（半小时）使文档口径同步入库。

**下轮路由**：待用户指示——审计轮（重点：P0-14 若已做则抽核）或工作轮（首选 P0-14；余额窗口开启则 P0-1 对数）。

## 9. 收尾声明

- 已执行并全部通过：git 现场校验、ruff、registry 动态计数、py_taint/守卫走读、全量 pytest（2518+2）、demo（verified）、金标（十二连零回退）、doctor、前端三件套、rename E2E 两场景、GLM 探针、收口件三件套核对。
- 未执行：SECRET 掩码行为脚本单独重跑（全量套件内覆盖，已标注）；守卫变异验证重做（引用 P0-13 收口期实测记录）；soak（36/36 已收官，W29 drain 上线后无新长稳窗口诉求）。
- 遗留问题：F11-R1（P2 已申报）、P0-1（余额阻塞）、W24-E~W30 待提交打版。
