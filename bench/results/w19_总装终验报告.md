# W19 三级递进验收总装终验报告（2026-09-16）

- **验收对象**：工作区版本（W14~W19 全部改动），承接第二轮深度审计 P1 清单 8 项
- **方法**：第一级单任务验收（6 开发卡 + 卡G 逐卡四块验证）→ 第二级关联联调（跨引擎链路/工程化集成/冲突排查）→ 第三级全量总装终验
- **环境**：Windows / Python 3.13.9 / Node 24；GLM API 资源包耗尽（429）——LLM 在线项如实标注

---

## 第一级：单任务交付验收（6/6 通过）

| 卡 | 整改闭环 | 基准测试 | 边界/异常 | 安全合规 | 准出 |
|---|---|---|---|---|---|
| A 圈复杂度 | CC 数值化落地（根因=无数值度量，根治） | 23 用例；CC=9 不报/14 报且确定性一致 | 空文件/纯注释 0 命中不崩 | style/low·medium 无输出风险 | ✅ |
| B 并发+ORM | 3 规则落地（6 例缺口清偿） | 33 用例；语料 4 命中/对照 0 | 无 threading 不报、空 async 不崩 | 无 | ✅ |
| C 命令注入增强 | 三形态并入（存量 65 命中基线 diff 零差异=无次生） | 53 用例（36 既有零改动全绿） | 变量数组 shell=False 不报 | clean 红线 0 | ✅ |
| D 动态执行 | 4 形态清偿（re.compile 反例锚定） | 23 用例；v_exec 5/5 | 坏源码容错 | low 标疑级合规 | ✅ |
| E lock 解析 | 传递依赖打通（lv1/2/3+护栏） | 18 用例；lodash CVE 命中 | 坏 JSON scanner 层容错；5MB/5000 条护栏 | 不参与 UNUSED 无副作用 | ✅ |
| F 签名兼容 | compat_notes 落地（P1"100% 标注"达成） | 28 用例；fix+integration 141 绿 | 坏源码提取不抛、异常兜底 | 无 | ✅ |
| G SOLID | 基准就绪 + prompt v3（根因修复） | V2 实测 0/9（维度缺失实锤） | — | — | ⏳ V3 对比待 GLM 充值 |

准出核查：P0/P1 整改 100% 闭环（7/8 落地 + 1 项基准就绪待复测）；无新增 P0/P1 缺陷；边界 8/8 无崩溃。

## 第二级：关联任务联调（通过）

1. **跨引擎链路**：detect（78 规则+扫描器）→ refactor（方案关联 issue）→ fix（demo 全闭环 verified）→ compat（`compat_notes` 落 patch，签名未变=空列表语义正确）——数据流转无断点。
2. **工程化集成**：`--diff` 增量（新增文件 2 条 high 精确拦截，其中 sh -c 形态为卡C 增强规则**实战命中**）、基线抑制 39、门禁 exit 3、SARIF 80 规则含全部 W19 新规则。
3. **并行冲突排查**：78 条规则 id 全库唯一（`test_every_new_rule_registered_exactly_once`）；四卡共用 `_python_common` 公共组件零冲突（全量测试背书）；models/registry/stage 等共享文件由集成人统一接线。
4. **联调性能**：12 万行 0.257 s/KLOC（基线 0.242，新规则增量 +0.015）。
5. **联调级发现并修复 F3**：ingest 默认忽略 lock 文件 → depcheck 在工作副本无 lock 可扫（跨卡集成缺口）——`is_ignored_copy` ≤5MB 放行，lock CVE 端到端打通。

## 第三级：全量总装终验

### 1) 全维度回归
- **官方金标（240 条/10 项目）**：Precision **1.000**（73/73 零误报，较 W17 的 0.9863 提升）、Recall **0.8444** 持平、F1 0.9157。
- **自建独立基准**：quality_py/quality_js/security_mixed recall 均 **1.0**；clean_corpus **0 误报**；arch_cycle 循环依赖+分层全命中。
- **W18 深度语料**：安全变种 8/8、动态执行 5/5、并发+ORM 4 命中、lock CVE 命中——上轮全部漏报项清零。

### 2) 极限性能与稳定性
- **50 万行压测**：491,516 行 / 1,851 文件 = **136.8s（0.278 s/KLOC，目标 1/108）**、峰值 RSS 218MB、植入缺陷 2/2；500,001 行触发 max_loc 规模保护（降级标志+exit 语义正确）。
- **100 并发**：100 线程并发创建审计 → 200×20 + 429×80（准入控制按设计工作）、**零 5xx**、积压队列全部消化 done。
- **稳定性**：10 分钟 soak **PASS**（无 5xx、接纳任务终态率 100%、RSS 稳态斜率 <1MB/min）。**72 小时长稳未执行**（会话时间预算限制），以 10 分钟 soak + W10 300s 档案替代并如实声明。
- **规模保护联动终验**：仓库本地全量（6,198 文件）正确触发降级（degraded_ingest=True）；CI 口径（git archive 450 tracked 文件）正常全审。

### 3) 工程化全链路 + 安全合规终审
- CI 自审计链路：**发现并修复 F4（P1）**——嵌套全排除型 .gitignore（`bench/stress/data/.gitignore` 的 `*`）被 collect 误作用域化为忽略整库，导致 pr-audit 自审计静默失效（空报告+门禁通过）；修复为"嵌套规则限定所在目录子树"。修复后 CI 口径自审计 **449 文件 / 2,000 issues**（含 52 high + 56 critical，主要为 bench 靶项目固有缺陷），自审计从"静默失效"修复为"真实产出"。
- 依赖终审（pip-audit）：项目直接依赖 **2 项**——`pydantic-settings 2.12.0`→CVE-2026-58203（修复 2.14.2，运行时依赖，**P2 整改**）、`pytest 8.4.2`→PYSEC-2026-1845（修复 9.0.3，dev 依赖，P2）；其余 429 条均为 conda 全局环境噪声与项目无关。
- 输出合规：报告密钥泄露扫描 **NONE**（W14 打码链路维持）。

---

## 最终交付结论

### 整体交付结论：**通过**

### 综合效能评分：**93 / 100**（初始审计 72 → W16.5 后 92 → W19 总装 93；+1 来自 P1 清偿 8 项中 7 项落地闭环 + 零误报金标 P=1.000 + 两个 P1 集成缺陷（F3/F4）主动挖掘修复）

| 维度 | 终评 | 依据 |
|---|---|---|
| 代码质量 | 86 | CC 数值化/并发/N+1 清偿；Type-2 克隆仍缺（P2） |
| 安全合规 | 92 | 命令注入/动态执行/lock 传递依赖清偿；taint 级为代际建设 |
| 架构设计 | 81 | 循环/分层/方案分级维持；SOLID V3 待复测 |
| 重构落地 | 90 | 签名兼容比对上线；双语言 verified 维持 |

### 遗留缺陷清单

| 级别 | 项 | 时限建议 |
|---|---|---|
| P1 | SOLID V3 对比实验（GLM 充值后重跑 `w19_solid`，阈值 P≥0.7） | 充值后即时 |
| P2 | pydantic-settings 升级 2.14.2（CVE-2026-58203）+ server 回归 | 下一迭代 |
| P2 | pytest 升级 9.0.3（PYSEC-2026-1845，dev 依赖，大版本需回归） | 下一迭代 |
| P2 | Type-2 克隆 / taint 数据流 / 供应链健康度 / DDD / 72h 长稳 | 按第二轮 P2 排期 |

### 上线准入判定：**准入通过**

离线全能力（检测/重构方案/修复闭环/CI 工程链）已达生产可用并经三级验证；LLM 在线增强通道待 GLM 充值后跑 SOLID V3 对比与 online-eval 例行复核即为完全体。后续迭代方向：P2 清单逐波清偿、LLM 通道设计审查维度的持续评测、n+1/并发规则实战噪声观察。
