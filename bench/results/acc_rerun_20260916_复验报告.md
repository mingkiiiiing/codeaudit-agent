# 三级递进验收独立复验报告（2026-09-16，W14~W20 工作区）

- **定位**：对同日《W19 三级递进验收总装终验报告》（93/100）做独立复验——以实测数据复核其三级结论，确认工作区处于可打版 0.7.0 的通过态
- **方法**：全量重跑 + 定向抽样复现 + 门禁问题逐条代码级定性；GLM 429（余额不足）如实贯穿
- **补充事实**：工作区实际含 **W20** 改动（py_security_ops 2 规则 / pyproject W20-B 依赖清偿），规则库 78→**80** 演进已实测确认

## 第一级：单任务验收复验 ✅

| 项 | 实测 | 结果 |
|---|---|---|
| 卡A~F 定向单测 | 288 passed / 10.0s（complexity/concurrency/orm/dynamic_exec/depcheck/compat/security_ops） | ✅ |
| 全量 pytest | **1845 passed / 276.97s，零失败**（pytest 9.0.3 下，5 warnings 均为预期） | ✅ |
| 规则库 | 注册表实测 **80 条、id 全库唯一零重复**（80 = 78 + W20 安全运营 2） | ✅ |
| ruff | All checks passed | ✅ |
| 卡G SOLID | prompt V3 在位（单一职责/开闭/依赖方向/接口设计四维度）、`D:/acc_tmp/eval/w19_solid(+_v3)` 语料在位；V3 对比因 GLM 余额不足**未执行**（继承遗留） | ⏳ |

## 第二级：关联联调复验 ✅（含新发现）

- **跨引擎闭环**：demo 8.0s 全链路（检测→verified Patch→生成单测 5 passed→三格式报告）。
- **金标独立复现**（10 项目/240 条）：**P=1.000 / R=0.844 / F1=0.916**，与 W19 报告逐位一致。
- **工程化门禁端到端**：`audit-self`（--diff origin/main，pr-audit.yml 同口径）实测 54 变更文件 / 700 issues / exit 3 + SARIF 82 规则（80 注册表 + PY-CLONE + DEP-UNUSED，注册表零缺失）。
- **门禁 13 个 critical/high 逐条定性（代码级）**：6 critical 全部为形态误报（taskstore SQL 的 f-string 仅拼常量片段/`?` 占位符且值全参数化 ×4、config.py 环境变量*名* ×1、测试假密钥夹具 ×1）；7 high 中 6 为 subprocess"手动检查 returncode"既有模式（含 2 测试），1 为 taskstore 驱逐循环逐行 DELETE 的真实 perf 提示（SQLite 小批量，可接受）。**无真实 SQL 注入、无真实密钥**。
- 🆕 **F5（P2）degraded 字段撞车**：refactor 阶段事件载荷 `"degraded": 5`（LLM 增强降级**计数**，refactor/llm.py stats）与 R4-10 done 事件 `"degraded": True`（ingest 失败标志）同名——`cli.py` 的 `any(event.get("degraded"))` 把计数误判为降级标志，对 LLM 降级场景误打"[警告] ingest 未成功"（实测复现：小样本无该警告、全量自审计触发；stats.degraded_ingest 始终 False，门禁判定不受影响）。建议修复 cli 判断侧（`is True` 或改查 done 事件），事件契约不动。

## 第三级：总装终验复验 ✅

- **性能/稳定性**：soak 120s 冒烟 **PASS**（无 5xx、接纳任务终态率 100%、RSS 斜率 <1MB/min）；50 万行 136.8s/218MB、100 并发零 5xx 沿用同日同树 W19 实测；**72h 长稳未执行**（继承声明）。
- **依赖遗留清偿确认**：W19 两项 P2 已由 W20-B 落地——pyproject 声明 `pydantic-settings>=2.14.2`、`pytest>=9.0.3`，环境版本一致，pytest 大版本回归由本轮 1845 绿直接覆盖。**该两项遗留关闭**。
- **输出合规**：全报告密钥扫描唯一命中为金标靶项目注入固件 `_inj_hardcoded_secret_2` 的假密钥（被检出的缺陷内容本身），无真实泄露。
- **文档同步**：🆕 F6（P2）README 第 285 行仍写"78 条静态规则"，与第 25 行"80 条"及注册表实测矛盾。

## 最终结论

**整体交付结论：通过（维持 93/100）**——独立复验未发现 P0/P1 级缺陷与能力回归；金标/全量回归/闭环 demo/门禁/SOAK 关键指标逐位复现。

| 级别 | 项 | 处置建议 |
|---|---|---|
| P1 | SOLID V3 对比实验 | 继承遗留，GLM 充值后重跑 `w19_solid`（阈值 P≥0.7） |
| P2 | F5 degraded 字段撞车（cli 误报降级警告） | 下一迭代，改 cli 判断侧一行 |
| P2 | F6 README:285 规则数 78 未同步 80 | 打版 0.7.0 前顺手改 |
| P2 | 自审计门禁对自身生产代码形态误报（6 critical 全误报→PR 门禁必红） | 为 4 处 SQL 构建/2 处 subprocess 加 inline suppress + 理由注释，或规则白名单化参数化占位符形态 |
| P2 | Type-2 克隆 / taint / 72h 长稳 等 | 按第二轮审计 P2 排期 |

**上线准入判定：维持准入通过。** 打版 0.7.0 前建议顺手关闭 F6；其余 P2 不阻塞。

## 复验产物与副作用披露

- 新增：`bench/results/acc_rerun_goldset_20260916.md`（金标复现报告）
- 覆盖：`bench/results/soak_w10.md` 被本轮 soak --quick 按工具默认输出路径覆写（该文件为历次 soak 惯例输出位，前值系上一会话同类 PASS 报告，W19 权威数据在其终验报告中保留）
- `.codeaudit/`（gitignored）下生成自审计报告与任务工作区；`D:/acc_tmp/acc_repro*` 为复现实验产物（仓库外）
