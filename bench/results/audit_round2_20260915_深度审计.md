# 第二轮全维度深度审计报告（W18，2026-09-15）

- **审计对象**：工作区版本（v0.6.0 + W14~W17 全部未提交改动），承接第一轮专项验收（72/100）与 W16/W16.5/W17 清偿后基线（92/100）
- **对标基准**：SonarQube Enterprise（复杂度/坏味道/技术债）、Snyk（依赖/供应链）、Checkmarx（数据流/利用链）高阶能力
- **方法**：回归闭环（8 项整改逐项复测）→ 深度专项（5 组边缘语料 42 条标签逐行归因，规则源码级设计校准）→ 工程验证（全量 pytest / ruff / 端到端）
- **数据可复现**：语料生成 `D:/acc_tmp/gen_w18.py`、评测产物 `D:/acc_tmp/eval/w18_*`

## 〇、回归闭环验证（上一轮整改项落地效果）

| 项 | 整改内容 | 复测结果 |
|---|---|---|
| R1 | 超限降级 degraded_ingest 标志（W17-F1） | PASS（JSON 显式 True） |
| R2 | PY-PII-SQL 明文入库 | PASS（L11 精确命中） |
| R3 | CFG-SECRET 配置密钥 | PASS（4/4） |
| R4 | 克隆+死代码扫描器 | PASS（双规则在位命中） |
| R5 | 重构方案 priority/effort | PASS（全方案带字段） |
| R6 | 分层违规规则 | PASS（api→dao 穿透命中） |
| R7 | 依赖种子库 CVE + DEP-UNUSED | PASS（默认离线零触网） |
| R8 | 循环依赖方案 P0/8.0h | PASS |

**整改闭环率：100%（W17 唯一发现 F1 已根治并复测；3 项备忘中 2 项关闭、1 项升级为建设项见 §三.2）；未发现任何整改引入的次生误报/漏报/回归。**

---

## 一、代码质量审计深度验证与专项测试

### 1.1 编码规范符合性
- 【现状判定】已达标（基线能力）／未覆盖（团队规范语义层）
- 【审计结论】回归：命名规范（PY-NAMING-STYLE/PY-PINYIN-NAMING）持续生效，误报 0。深度实测：**自定义规则 API 真实可用**——`register_rule(CustomRule)` 注入后自定义组织规范（CORP-NAMING-STRIP）在扫描中正确命中（PASS）；配置文件顶层键生效、未知键警告、不存在的显式配置文件均有明确中文警告。**本轮发现并修复 F2**：`.codeaudit.toml` 自动发现此前仅查 CWD，README 承诺"自动发现项目根"不成立——从其他目录（及 serve 常驻场景）审计时配置静默失效；已修复为"CWD 优先、回退被审项目根"（CWD 行为零变化 + 2 回归用例）。业务语义校验（命名与业务语义一致性、注释与逻辑同步性）需 LLM 深度语义通道，当前无专项基准——列为建设项。
- 【优先级】F2 修复=P0（已完成）；业务语义审计基准建设=P2

### 1.2 代码坏味道治理
- 【现状判定】部分达标
- 【审计结论】回归：克隆（Type-1）与死代码零回归。深度实测：**Type-1 克隆 100%、Type-2（改名+常量变化）0/2**——归一化只替换 NUM/STR 不归一标识符，语义级重复不识别（量化证实）；死代码的动态引用防御全部正确（装饰器注册/字符串引用/`__all__`/`globals()` 提及均豁免，0 误报），并正确捕获死代码调用链（`_dispatch_entry` 链头零引用即报）。**圈复杂度无数值化**：人工标定 CC=9 的函数 0 告警（长函数 80 行/深嵌套 4 层阈值均未触发）——对标 SonarQube 的 CC>10 告警与认知复杂度（cognitive complexity）能力缺失。极端场景（超长链式调用、多层三元）无独立度量。隐性坏味道（过度抽象、异常处理不一致、模式错用）依赖 LLM 通道无专项基准。
- 【优先级】圈复杂度数值化（tree-sitter AST 可达式实现，阈值可配默认 10）=**P1**；Type-2 克隆（token 级归一化标识符）=P2；隐性坏味道基准=P2

### 1.3 性能与资源效率
- 【现状判定】部分达标（行级反模式强；语义级资源分析空白）
- 【审计结论】回归：既有性能规则零回归。深度实测（6 例已知缺陷语料）：**并发类 0% 检出**——无锁 `+=` 竞态、嵌套锁死锁序、`threading` 风险全漏（无任何并发规则）；**DB 深度 0%**——ORM N+1（`session.query().get()` 循环）漏（IO-IN-LOOP 仅认 `.execute/open/urlopen`）、每请求重建 Engine 漏、长事务漏；实例级缓存只增不减泄漏漏。静态集合类泄漏仅在模块级 `_` 变量场景被死代码间接覆盖。
- 【优先级】并发三规则（无锁共享可变状态/双重检查锁/time.sleep 在 async）=**P1**；ORM N+1（`session.query().get|filter().first` 循环识别）=**P1**；资源生命周期（Engine/socket 未关闭）=P2

### 1.4 可测试性与可维护性
- 【现状判定】部分达标
- 【审计结论】回归：健康分公式复算一致（上轮 10/10 内容项维持）。深度审计：单测质量评估（有效性/边界覆盖/Mock 合理性、"为覆盖而覆盖"识别）无能力——testgen 只生成不评分；变异测试（mutmut/cosmic-ray 级）未集成；变更影响分析（基于既有调用图的受影响测试推荐）基础设施已在（符号表+调用图）但未暴露为功能。可维护性评分仍为健康分代理指标，与维护工作量的相关性无实证校准。
- 【优先级】调用图驱动的"变更影响面"报告（数据已在索引中，工程量小）=**P1**；变异测试集成=P2；可维护性模型校准=P2

---

## 二、安全合规审计深度验证与专项测试

### 2.1 代码原生安全漏洞
- 【现状判定】已达标（行级口径内）／部分缺口（数据流与语义级）
- 【审计结论】回归：8/8 应检出变种全命中（%多值、f-string 嵌套调用、join 拼接 IN、多行括号拼接续体行、伪净化后拼接行），**误报 0**；反序列化变种泛化 3/3（`pickle.load`/`yaml.unsafe_load`/`dill.loads` 全覆盖）；`subprocess -c` 列表形态被 PY-SUBPROCESS-WITHOUT-CHECK（bug/high）异规则捕获（语义部分覆盖）。**量化漏报（9 例）**：间接动态执行 4（`getattr(builtins,"eval")`/`__import__`/`compile`/`globals()[m]()`）、命令执行语义缺口 2（`Popen(cmd, shell=True)`/`os.execv`）、SQL 跨语句数据流 2（常量模板跨行 `.format`、常量前缀再拼接）、净化函数穿透 1（`replace(";","")` 伪净化被行级命中但未识别"净化不充分"语义）。利用链（source→sink 跨函数）分析无能力——Checkmarx 级 taint 引擎为代际差距，LLM tools 通道可部分补位（已真跑可用）但无专项基准。
- 【优先级】`Popen(shell=True)`/`os.execv` 并入命令注入规则=**P1**（规则内改动小、价值高）；动态执行四形态规则（`__import__`/`compile` 直接报，`getattr` 间接标疑）=**P1**；跨语句 SQL 常量拼接（`x = "SQL..."; y = x + v` 两行启发）=P1；taint 数据流引擎=P2（代际建设，建议 LLM tools 通道基准先行）

### 2.2 依赖组件安全
- 【现状判定】部分达标（直接依赖达标；传递依赖空白）
- 【审计结论】回归：种子库 CVE/DEP-UNUSED/OSV opt-in 全部零回归。深度实测：**间接依赖穿透 0%**——`package-lock.json` 中 lodash 4.17.20（CVE-2021-23337）实测漏报（lock 文件不解析）；供应链攻击（依赖投毒/依赖混淆/typosquatting）无能力；依赖健康度（维护活跃度/弃用风险）无能力（OSV 在线模式可部分补位但未做健康度字段）。Snyk 级"传递依赖图 + reachability"为代际差距。
- 【优先级】package-lock.json / poetry.lock / Pipfile.lock 解析并入 depcheck（结构化 JSON 解析，工程量可控）=**P1**；typosquatting 启发式（编辑距离 ≤2 的知名包名）=P2；健康度字段（OSV 元数据扩展）=P2

### 2.3 数据安全合规
- 【现状判定】部分达标
- 【审计结论】回归：PII 双规则（日志+入库）零回归、脱敏输出 4/4 维持。深度审计：全链路追踪（产生→传输→存储→使用→销毁）需 taint 基础设施（同 §2.1 缺口）；可逆脱敏识别（如仅尾号掩码可逆）无能力；跨境合规（数据出境标注）无能力——均属 LLM 语义通道+规则辅助的混合建设项。
- 【优先级】脱敏充分性启发式（掩码位数/可逆形态规则）=P2；全链路追踪随 taint 基建=P2

### 2.4 运维安全风险
- 【现状判定】已达标（配置文件密钥）/部分达标（API 安全）
- 【审计结论】回归：CFG-SECRET 4/4、占位豁免正确。深度审计：默认口令库（admin/admin 等常见默认凭据）无规则；API 安全（未授权路由/限流缺失）需框架感知（FastAPI/Flask 路由表）——当前架构可扩展（AST 提取装饰器路由+中间件分析）未实现；日志伪造（CRLF 注入日志）无规则。
- 【优先级】默认凭据字典规则=P2；Web 框架路由安全扫描（装饰器级）=P2

---

## 三、架构设计审计深度验证与专项测试

### 3.1 模块与依赖架构
- 【现状判定】部分达标
- 【审计结论】回归：循环依赖 2/2、分层违规精确命中维持。深度审计：间接循环依赖（A→B→C→A）依赖图可达性——`_find_cycles` 为全图搜索理论覆盖（未构造三层环语料实测，属验证缺口非能力缺口）；分布式/微服务拓扑、模块腐化速率预测无能力（需历史数据基线）。
- 【优先级】三层间接环语料实测并入 CI=P2；腐化预测需 git 历史分析基建=P2

### 3.2 设计原则符合性
- 【现状判定】未整改（承接上轮备忘，升级）
- 【审计结论】SOLID/DDD/模式误用识别仍完全依赖 LLM 通道（tools 模式已真跑可用）——**专项评测基准至今未建**，85% 类目标无实测数据，本轮升级为正式建设项。架构一致性校验（代码 vs 设计文档）无能力。
- 【优先级】SOLID 违规评测基准（上帝类/接口膨胀/双向依赖各 10 例）+ tools 模式实测 = **P1**；DDD 评估=P2

### 3.3 技术债量化评估
- 【现状判定】部分达标（上轮 P0/P1/P2 分级与工时估算已闭环）
- 【审计结论】回归：priority/effort 全方案在位、P0/8.0h 正确。深度审计：技术债演进预测（git 历史驱动的累积速率）、债务利息量化（缺陷率/交付效率关联）无能力——需代码仓库时序数据基建；偿还路径已有 steps 字段但无投入产出比量化。
- 【优先级】git 历史热点分析（churn×complexity 交叉）=P2；ROI 量化=P2

### 3.4 扩展性与可演进性
- 【现状判定】部分达标
- 【审计结论】技术栈升级风险、可观测性成熟度（埋点完善度）、插件化瓶颈识别无专项规则。当前自身插件化基础良好（规则注册 API 实测可用——即本报告 §1.1 的自定义规则实证），作为"被审计系统"的插件化示范成立。
- 【优先级】可观测性埋点检查规则（logger/metrics 缺失于异常路径）=P2

---

## 四、智能重构落地深度验证与专项测试

### 4.1 重构方案生成
- 【现状判定】已达标（分级/工时闭环）
- 【审计结论】回归维持。深度审计：十万行级分阶段重构规划——perf_big（12 万行）实测产出 0 方案（无热点聚合场景），大规模规划能力未受压测（依赖真实热点项目语料）；风险预判为 steps 内定性描述，无结构化风险清单；投入产出量化有工时无收益数值。
- 【优先级】大规模重构方案压测（构造高耦合 10 万行语料）=P2；收益量化（复杂度下降折算）=P2

### 4.2 代码自动重构
- 【现状判定】已达标（PY/JS 双语言 verified 闭环）
- 【审计结论】回归：JS SQL 拼接→参数化 verified（2/2 测试绿）维持。深度审计：跨文件重构（提取公共类/跨模块抽取）单文件 diff 管线不支持多文件 patch（`git apply` 单 diff 可含多文件，但 Fix Agent 提示词按单文件上下文设计）——能力边界；重构副作用检测（性能退化）无基准对比钩子。
- 【优先级】多文件重构支持=**P1**（改造 Fix 上下文注入跨文件符号）；重构前后性能基准（pytest-benchmark 钩子）=P2

### 4.3 重构验证机制
- 【现状判定】已达标（三重验证 + 诚实降级）
- 【审计结论】回归：verified 路径双语言实测维持；needs-review 降级为 LLM diff 非确定性的正确兜底（上轮复现 2 次全 verified）。深度审计：语义等价性验证（AST 级语义 diff）无——语法+测试闸门之外的"逻辑等价"依赖测试充分性；接口兼容性校验（签名/返回结构破坏性变更）无专项检查——建议基于符号表做 patch 前后签名比对（数据已具备）。
- 【优先级】Patch 前后函数签名兼容性比对（符号表 diff）=**P1**；语义等价（AST 规范化比对）=P2

### 4.4 工程化协同能力
- 【现状判定】已达标（增量/基线/门禁/SARIF 全链路）
- 【审计结论】回归：本轮 F2 修复补齐"配置随项目走"的最后一块（serve 常驻场景配置生效）。深度审计：多人并行重构冲突预判（两个 patch 同文件竞争）——fix 阶段为串行处理无冲突场景；重构分支管理（分支创建/同步）超出工具边界属工作流规范。pr-audit.yml 自审计工作流在位。
- 【优先级】同文件多 patch 合并策略文档化=P2

---

## 第二轮整体审计结论

- **整改闭环率：100%**（上轮唯一整改项 F1 根治并复测；备忘 2 项关闭、1 项升级为 P1 建设项；8/8 回归全 PASS，未引入任何次生问题）
- **综合效能评分：92 / 100（维持）**——深度审计未发现已达标项的能力回归或新缺陷（安全变种 8/8+误报 0、死代码动态引用防御全对、自定义规则 API 可用），新增发现全部为**高阶能力缺口**（多数在第一轮已定性记录，本轮完成定量标定），不扣已有评分；新增 2 项真实缺陷（F1/W17 已计、F2 本轮）均当场修复闭环。
- **四维评分**：质量 85（Type-2 克隆 0/2、复杂度无数值 → 建设中）、安全 90（变种泛化强+误报 0；语义/数据流缺口定量 9 例）、架构 80、重构 88（双语言 verified 维持）。

## 遗留风险总览（按优先级）

| 优先级 | 项 | 量化证据 | 验收阈值 |
|---|---|---|---|
| **P1** | 圈复杂度数值化告警 | CC=9 样本 0 告警 | CC>10 触发、误差 ±1 |
| **P1** | 并发缺陷规则（竞态/锁/async sleep） | 6 例并发 DB 缺陷 0 检出 | 语料检出 ≥4/6 |
| **P1** | ORM N+1 识别 | session.query().get() 循环漏报 | 同型语料 100% |
| **P1** | 命令注入两形态（Popen shell=True / os.execv） | 2 例漏报 | 2/2 命中且误报 0 |
| **P1** | 动态执行四形态（__import__/compile 直接报；getattr/globals 标疑 low） | 4 例漏报 | ≥2/4 命中 |
| **P1** | lock 文件传递依赖解析 | lodash CVE 漏报 | lock 内 CVE 100% 匹配 |
| **P1** | SOLID 评测基准 + tools 模式实测 | 基准未建 | P≥0.7（LLM 口径） |
| **P1** | Patch 签名兼容性比对 / 多文件重构 | 无该能力 | 接口破坏性变更 100% 标注 |
| P2 | Type-2 克隆、SQL 跨语句常量拼接、taint 引擎、lock 之外供应链、健康度、DDD、腐化预测、变异测试、可观测性/默认凭据/日志伪造规则、大规模重构压测 | 见各节 | 分批迭代 |

## 复测建议

1. P1 各项随下波（W19）并行清偿后，按上表阈值逐项复测——语料已备好（`D:/acc_tmp/gen_w18.py` 42 标签 + `w18_cc`），直接复用。
2. LLM 通道专项（SOLID 基准、语义审计）需消耗 token，建议与 0.7.0 打版后的 online-eval 例行任务合并执行。
3. 本轮全部实测中间产物：`D:/acc_tmp/eval/w18_*`；金标回归与全量 pytest（1656 passed / 295s、ruff 绿）已复核。
