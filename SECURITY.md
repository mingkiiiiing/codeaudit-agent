# 安全政策

## 支持版本

| 版本 | 支持状态 |
|---|---|
| 0.2.x | :white_check_mark: 支持中 |
| 0.1.x | :x: 已停止维护，请升级到 0.2.x |

## 报告漏洞

**请不要用公开 issue 报告安全漏洞。**

优先渠道：GitHub **Security → Advisories → Report a vulnerability**（私有漏洞报告，仅维护者可见，便于跟踪与致谢）。

备用渠道：邮件 <security@example.com>（占位邮箱，正式启用前以 GitHub Security Advisories 为准）。

响应约定：

1. 72 小时内确认收到；
2. 7 天内给出初步评估（是否成立、严重程度、影响版本）；
3. 修复在最新支持版本发布，发布时通过 Security Advisory 公示与致谢（除非你要求匿名）。

## 范围说明

本项目是**本地运行**的开发者工具：不设任何服务器、不收集代码与遥测。用户代码仅发送至**用户自行配置**的 LLM API 端点；`GLM_API_KEY` 只从本地环境读取，不写入报告与日志。因此漏洞面主要集中在：

- **本地执行面**：沙箱执行器（`audit/sandbox/`，运行项目现有测试与生成的单测）、`git apply` 补丁应用——能诱导越界执行任意命令的问题视为高危；
- **报告注入**：被审计源码中的内容未经转义进入 HTML 报告（XSS，尤其在把报告部署到静态站点时）；
- **密钥处理**：报告中检测到的密钥未按承诺打码、密钥被写入非预期位置；
- **依赖链**：依赖中的已知漏洞（Dependabot 每周巡检，欢迎补充报告）。

以下不属于漏洞：LLM 端点本身的可用性与合规问题、离线模式下的检测误报 / 漏报（请走 issue 模板）。
