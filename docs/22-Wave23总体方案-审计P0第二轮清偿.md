# Wave 23 总体方案——审计 P0 第二轮清偿

> 2026-09-17 ｜ 状态：已完成（联调实测数据见 §4） ｜ 前置：第三轮全维审计（2026-09-17，综合 66/100，`bench/results/audit_round3_20260917_复审.md`，P0 八项新清单）
>
> 工作流：审计轮（提示词一）→ 工作轮（提示词二）循环；本轮为工作轮，多卡并行（文件所有权互不相交，并发 ≤3-4）+ 集成人统一联调收口。

## 0. 背景与选卡依据

第三轮审计 P0 八项 → 本轮四卡：

| 审计条目 | 一句话 | 本轮处置 |
|---|---|---|
| P0-2 | apply-to-source 预览确认回路——重构闭环断头在用户手工 `git apply` | **卡 A（本轮）** |
| P0-3 | AST 断供修复——82 规则全行级，`engine.py` tree=None 一处接线（tree-sitter 已在依赖） | **卡 B（本轮）** |
| P0-5 | 测试覆盖盲区识别——数据已在索引，只差反查与呈现 | **卡 C（本轮）** |
| P0-8 | commit message / PR 描述——修复后仍要用户手写提交信息 | **卡 D（本轮，依赖卡 A）** |
| P0-1 | 成本三项翻默认 | 跳过：验收依赖 GLM 充值在线对数（429 余额不足，同 docs/21 §5） |
| P0-7 | 模型路由 + fallback | 跳过：同上（429 单点实证需在线复现） |
| P0-4 | Java 语言包 | 顺延：声明依赖 P0-3 落地（本轮已解），下一工作轮候选 |
| P0-6 | resume + 报告历史对比 | 顺延：下一工作轮候选 |

## 1. 任务卡与所有权

| 卡 | 文件 | 交付物 |
|---|---|---|
| W23-A | `cli.py`（apply 子命令）、`audit/fix/applyer.py`（新）、`server/app.py`、`tests/integration/test_gray_release.py`、tests | `codeaudit apply <audit_id> [--patch N] [--all-verified] [--yes]`：**默认 dry-run 预览不落盘**，`--yes` 才写入；服务端 `POST /api/audits/{audit_id}/patches/{patch_index}/apply`（语义与 CLI 一致）；原文指纹（Patch.target_sha256，生成侧补记）：目标文件当前内容 sha256 与审计时不一致 → 拒绝该 patch（防覆盖用户后续改动）；任何拒绝 → 整体不落盘（all-or-nothing）；老报告无指纹 → dry-run 可预览、落盘被拒绝（诚实降级）；gray_release API 路由冻结断言随新端点同步更新 |
| W23-B | `audit/detect/ast_util.py`（新）、`audit/detect/engine.py`、`audit/detect/rules/py_none_deref.py`（新）、`audit/detect/rules/python.py`/`py_concurrency.py`/`py_orm.py`、tests | AST 接线：build_rule_contexts 与 `_scan_file_worker`（并行侧）同口径为 python 文件 tree-sitter 解析传 `ctx.tree`（同源码→同树→同命中，行为等价铁律不破）；失败/超时/无解析器/`CODEAUDIT_DISABLE_AST=1` 降级 None 绝不阻断，接线统计写 `extra["ast_wiring"]`；三规则（SQL 注入 / 并发 2 / ORM N+1）AST 佐证只提升置信（`meta["ast_confirmed"]`、confidence 0.7→0.8），佐证失败或 tree=None 命中原样保留、命中行号集合与无 AST 完全一致（金标不回退硬约束）；新规则 PY-NONE-DEREF（bug/high，AST-only：同函数 None 赋值→解引用，tree=None 不产命中），规则库 82 → **83 条** |
| W23-C | `audit/report/testcoverage.py`（新）、`audit/models.py`、`audit/report/builder.py`、`audit/report/render.py`、tests | `untested_hotspots` 测试盲区识别：critical/high 命中且非测试文件的公开符号，经索引 call_edges `callers` 方向反查零测试触达 → 盲区；输出 TOP 20 + 占比（盲区数/eligible 数）+ methodology（诚实局限随节渲染：动态调用 getattr/反射偏悲观、索引跨文件解析率 resolved_ratio 偏低时误差放大，仅供测试补充排期参考）；三格式（md/html/sarif 通道外的 json/md/html 渲染）走现有 extra/stats 通道。**契约文件例外报备**：`AuditReport.untested_hotspots` 为带默认值可选字段（旧报告 from_dict 兼容，schema 不破坏） |
| W23-D | `audit/fix/commitmsg.py`（新）、`cli.py`（apply 段扩展）、tests | P0-8：`apply` 命令新增 `--commit-message` / `--pr-description` 开关（纯字符串拼接、零 LLM token）；单补丁模板 `fix(<category>): <title> (<rule_id>)`，多补丁按 (category, rule_id) 去重；rule_id 经 Issue.evidence 的 `rule:PY-XXX` 证据行反查（与 SARIF 导出同款约定，契约零变化），反查不到诚实降级 `fix(general): <rationale 截断 72 字符> (unknown-rule)`；PR 描述含变更摘要/涉及规则/逐补丁验证状态表（apply_status、tests_run=0 显式标"未运行"、compat_notes 非空才列）；dry-run 输出带「（预览）」标注，不改 apply 既有行为与退出码 |

并行派发顺序：A/B/C 三卡并行（所有权互不相交）→ A 完成后派 D（依赖 apply 选中补丁集语义）→ 集成人统一联调。

## 2. 设计决策记录

1. **apply 默认 dry-run**：对源码写入是本项目第一个"出 audit 报告之外副作用"的入口，安全默认与 W22-D（非回环强制 token）同哲学——危险动作显式 `--yes`。
2. **sha256 指纹防覆盖 + all-or-nothing**：审计与应用之间用户可能手改源码；逐 patch 校验、任一拒绝整体不落盘，避免半应用状态（半应用比不应用更危险）。
3. **AST 只做置信佐证、不做命中删改**：行为等价铁律（金标 P=1.000/R=0.844 逐位一致为验收门）——AST 佐证失败或 tree=None 时行级命中原样保留，行级逻辑仍是兜底路径；只有新规则 PY-NONE-DEREF 是 AST-only（其命中集合增量、不触碰存量）。
4. **rule_id 走 evidence 约定而非改契约**：Patch/Issue 模型无 rule_id 字段，models.py 是契约文件；detect 引擎写入的 `rule:XXX` 证据行已被 SARIF 导出消费，复用同款解析零契约变化。
5. **commit message 纯拼接零 token**：模板化输出确定性可测（15 用例），不引入 LLM 非确定性；needs-review/failed 状态如实进 PR 描述（不粉饰）。
6. **测试盲区只做 TOP 20 + methodology 诚实标注**：resolved_ratio（当前约 0.178 口径）偏低时反查两种误差都放大，能力边界写进渲染而非藏在实现里。

## 3. 已知边界（诚实声明）

- apply 只处理 Patch（fix 阶段产物）；RefactorProposal 类方案性建议仍走报告阅读路径。
- 老报告（无 target_sha256）不能落盘只能预览——重新审计后即可获得指纹。
- PY-NONE-DEREF 覆盖"同函数内直接 None 赋值流"，跨函数/属性路径传播不在本轮口径（AST-only 保守起步）。
- untested_hotspots 对动态调用（getattr/反射）悲观、对 resolved_ratio 敏感，节内已附 methodology 说明。
- commit message 的 category 取 Issue 既有分类，不做语义归并；unknown-rule 条目会出现在涉及规则清单（与摘要计数口径一致）。

## 4. 验收（集成人统一联调实测，2026-09-17）

- **逐卡定向**：W23 四卡新增测试合计 **106 用例**（A：applyer/cli_apply/server_apply——tests/unit/fix/test_apply_inside_repo.py 2 用例系 HEAD 既有沙箱用例非本轮产物；B：ast_wiring+ast_enhance 24 + none_deref 17；C：untested_hotspots 12；D：commitmsg 15）全部通过；`ruff check audit cli.py server` 绿。
- **联调发现并修复 2 项**：①registry 计数断言未含 W23 段（82≠83）——test_rules_ext.py 补 W23_RULE_COUNT=1；②卡 B 会话中断致 PY-NONE-DEREF 行为用例缺失（8 个样本文件在库但无测试驱动）——集成人实测 5/5 正例检出（行号逐一断言）、3/3 反例零误报、tree=None 全部不产命中后补 test_w23_none_deref.py（17 用例）。
- **断言演进 1 项**：test_engine `rc.tree is None` 为 AST 断供旧行为断言，按新契约改为条件断言（接线开启时 tree 非 None 且 degraded=0）。
- **统一联调**：全量 pytest **2036 passed**（基线 1930 + 净新增 106，4 分 51 秒）、ruff 绿、demo 离线闭环 verified（9.8s）、金标 10 项目 240 标签 **P=1.000 / R=0.844 / F1=0.916**（与基线逐位一致，卡 B"回退≤1pp"门零回退达成）、gray_release 集成 4 passed（含 apply 新端点冻结断言）。
- 阻塞项维持：P0-1/P0-7 与 W22-B 默认值翻转均卡 GLM 账户余额（HTTP 429），充值后按 docs/21 §5 判据复跑。

## 5. 下一轮入口

- 按审计↔工作循环：本轮工作收口后，下一轮 = 新开 GLM-5.3 对话贴提示词一（第四轮审计），重点即 W23 四卡质量复审 + 上轮 P0 未清偿项复核（P0-1/4/6/7）。
- P0-4（Java）已解除对 P0-3 的依赖，为下一工作轮首候选；P0-6（resume + 报告对比）次之。
